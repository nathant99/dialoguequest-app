# Agent Safety — Reaffirmed (2026-07-06)

Direction: **session note → future Claude Code sessions**. Single-page reaffirmation of the Xcode-managed-files boundary, the staging + committing carve-out, and the canonical SPM-side work surfaces. Future sessions should read this once before touching the repo.

> **Reaffirmed 2026-07-06 (PR 1 of a 4-PR session-handoff Priority-pickup round)** by user-direct: *"critical: do not author/edit xcode-managed files including Xcode workspace file and Xcode scheme/test plan file. Instead, file a handoff doc with the user to do Xcode UI work. staging and committing Xcode-managed files is ok. Update CLAUDE.md, rules, docs about this."*. The boundary + carve-out remain canonical verbatim — exact same user-direct phrasing as 2026-07-05 and 2026-07-04 and 2026-07-03 and 2026-07-02 and 2026-07-01 and 2026-06-30 and 2026-06-29 and 2026-06-28 and 2026-06-27 and 2026-06-26 and 2026-06-25. Confirmed no drift in `CLAUDE.md` § Xcode Agent Safety or `.claude/rules/xcode-agent-safety.md` § "Staging + committing Xcode-managed files IS allowed" after a clean read this round. Round PRs in `Docs/IMPLEMENTATION_HANDOFF.md` seventeenth-mid-session entry (Track A docs reaffirm / Track B Priority R privacy-mode reset for the persisted emotional snapshot — Settings entry behind `ParentalGateChallenge` + new `emotionalSnapshotReset` analytics event / Track C Priority I ForgeKit module re-survey audit doc capturing per-module held-verdict trigger criteria / Track Z round-close + session handoff). The auto-cycle runs 4 times cleanly (branch → commit → push → PR → merge → verify). Neither user-GUI prereq from 2026-06-26 (ForgeKit pin bump + Localizable.xcstrings) has landed yet after 240h — Priorities B + C from the prior session handoffs remain blocked. This round closes Priority R from the 2026-07-05 brief and ships the held-module trigger criteria as a structured audit doc so future sessions don't re-survey the same held verdict every round. No Xcode-managed-file authoring; no new privileged surfaces beyond the existing handoffs.
>
> **Reaffirmed 2026-07-05 (PR 1 of a 4-PR session-handoff Priority-pickup round)** by user-direct: *"critical: do not author/edit xcode-managed files including Xcode workspace file and Xcode scheme/test plan file. Instead, file a handoff doc with the user to do Xcode UI work. staging and committing Xcode-managed files is ok. Update CLAUDE.md, rules, docs about this."*. The boundary + carve-out remain canonical verbatim — exact same user-direct phrasing as 2026-07-04 and 2026-07-03 and 2026-07-02 and 2026-07-01 and 2026-06-30 and 2026-06-29 and 2026-06-28 and 2026-06-27 and 2026-06-26 and 2026-06-25. Confirmed no drift in `CLAUDE.md` § Xcode Agent Safety or `.claude/rules/xcode-agent-safety.md` § "Staging + committing Xcode-managed files IS allowed" after a clean read this round. Round PRs in `Docs/IMPLEMENTATION_HANDOFF.md` sixteenth-mid-session entry (Track A docs reaffirm / Track B Priority M.4 stale-snapshot framing on parent dashboard `EmotionalSignalsPersistence.lastSnapshotAt` + "Last read N ago" line in "How writing felt" / Track C Priority N.3 experiment-cohorts section on parent dashboard reading `DialogueExperimentsService.variant(forExperimentID:)` per definition / Track Z round-close + session handoff). The auto-cycle runs 4 times cleanly (branch → commit → push → PR → merge → verify). Neither user-GUI prereq from 2026-06-26 (ForgeKit pin bump + Localizable.xcstrings) has landed yet after 216h — Priorities B + C from the prior session handoffs remain blocked. This round closes Priority M.4 + Priority N.3 from the 2026-07-05 brief. No Xcode-managed-file authoring; no new privileged surfaces beyond the existing handoffs.
>
> **Reaffirmed 2026-07-04 (PR 1 of a 4-PR session-handoff Priority-pickup round)** by user-direct: *"critical: do not author/edit xcode-managed files including Xcode workspace file and Xcode scheme/test plan file. Instead, file a handoff doc with the user to do Xcode UI work. staging and committing Xcode-managed files is ok. Update CLAUDE.md, rules, docs about this."*. The boundary + carve-out remain canonical verbatim — exact same user-direct phrasing as 2026-07-03 and 2026-07-02 and 2026-07-01 and 2026-06-30 and 2026-06-29 and 2026-06-28 and 2026-06-27 and 2026-06-26 and 2026-06-25. Confirmed no drift in `CLAUDE.md` § Xcode Agent Safety or `.claude/rules/xcode-agent-safety.md` § "Staging + committing Xcode-managed files IS allowed" after a clean read this round. Round PRs in `Docs/IMPLEMENTATION_HANDOFF.md` fifteenth-mid-session entry (Track A docs reaffirm / Track B Priority M.3 emotional-state descriptor on parent dashboard / Track C Priority N.2 `experiment_variant_assigned` analytics event at cold launch / Track Z round-close + session handoff). The auto-cycle runs 4 times cleanly (branch → commit → push → PR → merge → verify). Neither user-GUI prereq from 2026-06-26 (ForgeKit pin bump + Localizable.xcstrings) has landed yet after 192h — Priorities B + C from the prior session handoffs remain blocked. This round closes Priority M.3 + Priority N.2 from the 2026-07-04 brief. No Xcode-managed-file authoring; no new privileged surfaces beyond the existing handoffs.
>
> **Reaffirmed 2026-07-03 (PR 1 of a 4-PR session-handoff Priority-pickup round)** by user-direct: *"critical: do not author/edit xcode-managed files including Xcode workspace file and Xcode scheme/test plan file. Instead, file a handoff doc with the user to do Xcode UI work. staging and committing Xcode-managed files is ok. Update CLAUDE.md, rules, docs about this."*. The boundary + carve-out remain canonical verbatim — exact same user-direct phrasing as 2026-07-02 and 2026-07-01 and 2026-06-30 and 2026-06-29 and 2026-06-28 and 2026-06-27 and 2026-06-26 and 2026-06-25. Confirmed no drift in `CLAUDE.md` § Xcode Agent Safety or `.claude/rules/xcode-agent-safety.md` § "Staging + committing Xcode-managed files IS allowed" after a clean read this round. Round PRs in `Docs/IMPLEMENTATION_HANDOFF.md` fourteenth-mid-session entry (Track A docs reaffirm / Track B Priority M.2 wire `DialogueEmotionalStateProbe` into `PatterReactionService` for celebration suppression / Track C Priority N `ForgeExperiments` thin-wrapper adoption — module count 22 → 23 / Track Z round-close + session handoff). The auto-cycle runs 4 times cleanly (branch → commit → push → PR → merge → verify). Neither user-GUI prereq from 2026-06-26 (ForgeKit pin bump + Localizable.xcstrings) has landed yet after 168h — Priorities B + C from the prior session handoffs remain blocked. This round closes Priority M.2 (the standalone probe now has a real consumer) and adopts `ForgeExperiments` as the rationalized A/B harness alongside the existing `@AppStorage("dq.experiments.*")` flags. No Xcode-managed-file authoring; no new privileged surfaces beyond the existing handoffs.
>
> **Reaffirmed 2026-07-02 (PR 1 of a 4-PR session-handoff Priority-pickup round)** by user-direct: *"critical: do not author/edit xcode-managed files including Xcode workspace file and Xcode scheme/test plan file. Instead, file a handoff doc with the user to do Xcode UI work. staging and committing Xcode-managed files is ok. Update CLAUDE.md, rules, docs about this."*. The boundary + carve-out remain canonical verbatim — exact same user-direct phrasing as 2026-07-01 and 2026-06-30 and 2026-06-29 and 2026-06-28 and 2026-06-27 and 2026-06-26 and 2026-06-25. Confirmed no drift in `CLAUDE.md` § Xcode Agent Safety or `.claude/rules/xcode-agent-safety.md` § "Staging + committing Xcode-managed files IS allowed" after a clean read this round. Round PRs in `Docs/IMPLEMENTATION_HANDOFF.md` thirteenth-mid-session entry (Track A docs reaffirm / Track B last silent-`try?` site closure — `AnthologyGalleryView.indexInSpotlight` Spotlight pair / Track C Priority M ForgeEmotionAware adoption + thin `DialogueEmotionalStateProbe` wrapper / Track Z round-close + session handoff). The auto-cycle runs 4 times cleanly (branch → commit → push → PR → merge → verify). Neither user-GUI prereq from 2026-06-26 (ForgeKit pin bump + Localizable.xcstrings) has landed yet after 144h — Priorities B + C from the prior session handoffs remain blocked. This round closes Priority K to 100% (16/16 silent-fail sites wired) and grows the ForgeKit catalog from 21 → 22 modules. No Xcode-managed-file authoring; no new privileged surfaces beyond the existing handoffs.
>
> **Reaffirmed 2026-07-01 (PR 1 of a 5-PR session-handoff Priority-pickup round)** by user-direct: *"critical: do not author/edit xcode-managed files including Xcode workspace file and Xcode scheme/test plan file. Instead, file a handoff doc with the user to do Xcode UI work. staging and committing Xcode-managed files is ok. Update CLAUDE.md, rules, docs about this."*. The boundary + carve-out remain canonical verbatim — exact same user-direct phrasing as 2026-06-30 and 2026-06-29 and 2026-06-28 and 2026-06-27 and 2026-06-26 and 2026-06-25. Confirmed no drift in `CLAUDE.md` § Xcode Agent Safety or `.claude/rules/xcode-agent-safety.md` § "Staging + committing Xcode-managed files IS allowed" after a clean read this round. Round PRs in `Docs/IMPLEMENTATION_HANDOFF.md` twelfth-mid-session entry (Track A docs reaffirm / Track B Priority K silent-`try?` site sweep — 11 more sites wired across PatterCallbackService / AnthologyCollectionService / AnthologyGalleryView / AnthologyCurationView / PerformanceBoothView / DialogueWritingSessionActivity / VoiceActingCoachActiveSession / DialogueAudioExporter / Track C Priority L app-shell DebugLog lifecycle hooks via MCP `XcodeUpdate` / Track D Priority I ForgeDevelopmental adoption + thin `DevelopmentalCapacityProbe` wrapper / Track Z round-close + session handoff). The auto-cycle runs 5 times cleanly (branch → commit → push → PR → merge → verify). Neither user-GUI prereq from 2026-06-26 (ForgeKit pin bump + Localizable.xcstrings) has landed yet after 120h — Priorities B + C from the prior session handoffs remain blocked. This round picks up Priorities I + K + L from the 2026-07-01 brief and grows the ForgeKit catalog from 20 → 21 modules. App-shell edit goes through MCP `XcodeUpdate` per the synchronized-folder target convention. No Xcode-managed-file authoring; no new privileged surfaces beyond the existing handoffs.
>
> **Reaffirmed 2026-06-30 (PR 1 of a 5-PR session-handoff Priority-pickup round)** by user-direct: *"critical: do not author/edit xcode-managed files including Xcode workspace file and Xcode scheme/test plan file. Instead, file a handoff doc with the user to do Xcode UI work. staging and committing Xcode-managed files is ok. Update CLAUDE.md, rules, docs about this."*. The boundary + carve-out remain canonical verbatim — exact same user-direct phrasing as 2026-06-29 and 2026-06-28 and 2026-06-27 and 2026-06-26 and 2026-06-25. Confirmed no drift in `CLAUDE.md` § Xcode Agent Safety or `.claude/rules/xcode-agent-safety.md` § "Staging + committing Xcode-managed files IS allowed" after a clean read this round. Round PRs in `Docs/IMPLEMENTATION_HANDOFF.md` eleventh-mid-session entry (Track A docs reaffirm / Track B CastIllustrationsCoverageAudit value type + tests / Track C debug-only Settings diagnostics surface / Track D DialogueQuestDebugLog seam + silent-try? site wiring / Track Z round-close + session handoff). The auto-cycle runs 5 times cleanly (branch → commit → push → PR → merge → verify). Neither user-GUI prereq from 2026-06-26 (ForgeKit pin bump + Localizable.xcstrings) has landed yet after 96h — Priorities B + C from the prior session handoffs remain blocked, and this round picks up Priority J (CastIllustrationsCoverageAudit) from the 2026-06-30 brief + adopts the portfolio-canonical `DialogueQuestDebugLog` seam per `.claude/rules/debug-logging.md`. No Xcode-managed-file authoring; no new privileged surfaces beyond the existing handoffs.

> **Reaffirmed 2026-06-29 (PR 1 of a 4-PR session-handoff Priority-pickup round)** by user-direct: *"critical: do not author/edit xcode-managed files including Xcode workspace file and Xcode scheme/test plan file. Instead, file a handoff doc with the user to do Xcode UI work. staging and committing Xcode-managed files is ok. Update CLAUDE.md, rules, docs about this."*. The boundary + carve-out remain canonical verbatim — exact same user-direct phrasing as 2026-06-28 and 2026-06-27 and 2026-06-26 and 2026-06-25. Confirmed no drift in `CLAUDE.md` § Xcode Agent Safety or `.claude/rules/xcode-agent-safety.md` § "Staging + committing Xcode-managed files IS allowed" after a clean read this round. Round PRs in `Docs/IMPLEMENTATION_HANDOFF.md` tenth-mid-session entry (Track A docs reaffirm / Track F CastIllustrationsRegistry app-launch wiring / Track G fourth seasonal theme / Track Z round-close + session handoff). The auto-cycle runs 4 times cleanly (branch → commit → push → PR → merge → verify). Neither user-GUI prereq from 2026-06-26 (ForgeKit pin bump + Localizable.xcstrings) has landed yet after 72h — Priorities B + C from the prior session handoffs remain blocked, and this round picks up Priority F (registry populate at launch) + Priority G (round out the year of seasonal-theme windows) from the 2026-06-28 brief. No Xcode-managed-file authoring; no new privileged surfaces beyond the existing handoffs.
>
> **Reaffirmed 2026-06-28 (PR 1 of a 5-PR session-handoff Priority-pickup round)** by user-direct: *"critical: do not author/edit xcode-managed files including Xcode workspace file and Xcode scheme/test plan file. Instead, file a handoff doc with the user to do Xcode UI work. staging and committing Xcode-managed files is ok. Update CLAUDE.md, rules, docs about this."*. The boundary + carve-out remain canonical verbatim — exact same user-direct phrasing as 2026-06-27 and 2026-06-26 and 2026-06-25. Confirmed no drift in `CLAUDE.md` § Xcode Agent Safety or `.claude/rules/xcode-agent-safety.md` § "Staging + committing Xcode-managed files IS allowed" after a clean read this round. Round PRs in `Docs/IMPLEMENTATION_HANDOFF.md` ninth-mid-session entry (Track A docs reaffirm / Track B weekly-summary delta service + parent dashboard surface / Track C ForgeIllustrations adoption for cast portraits + book covers / Track D ForgeEvents seasonal-theme advisory + 5th bubble slot / Track Z round-close + session handoff). The auto-cycle runs 5 times cleanly (branch → commit → push → PR → merge → verify). Neither user-GUI prereq from 2026-06-26 (ForgeKit pin bump + Localizable.xcstrings) has landed yet after 48h — Priorities B + C from the prior session handoffs remain blocked, and this round picks up Priority F (weekly summary delta) + maximizes ForgeKit integration via the two new module adoptions (count 18 → 20). No Xcode-managed-file authoring; no new privileged surfaces beyond the existing handoffs.
>
> **Reaffirmed 2026-06-27 (PR 1 of a 4-PR session-handoff Priority-pickup round)** by user-direct: *"critical: do not author/edit xcode-managed files including Xcode workspace file and Xcode scheme/test plan file. Instead, file a handoff doc with the user to do Xcode UI work. staging and committing Xcode-managed files is ok. Update CLAUDE.md, rules, docs about this."*. The boundary + carve-out remain canonical verbatim — exact same user-direct phrasing as 2026-06-26 and 2026-06-25. Confirmed no drift in `CLAUDE.md` § Xcode Agent Safety or `.claude/rules/xcode-agent-safety.md` § "Staging + committing Xcode-managed files IS allowed" after a clean read this round. Round PRs in `Docs/IMPLEMENTATION_HANDOFF.md` eighth-mid-session entry (Track A docs reaffirm / Track B voice-pattern trend callback / Track C weekly summary service + parent dashboard surface / Track Z round-close + session handoff). The auto-cycle runs 4 times cleanly (branch → commit → push → PR → merge → verify). Neither user-GUI prereq from 2026-06-26 (ForgeKit pin bump + Localizable.xcstrings) landed since last round — Priorities B + C from the prior session handoff remain blocked, and this round picks up Priority F (voice-pattern callback) + completes the deferred weekly-summary rendering row. No Xcode-managed-file authoring; no new privileged surfaces beyond the existing handoffs.
>
> **Reaffirmed 2026-06-26 (PR 1 of a 5-PR session-handoff Priority-pickup round)** by user-direct: *"critical: do not author/edit xcode-managed files including Xcode workspace file and Xcode scheme/test plan file. Instead, file a handoff doc with the user to do Xcode UI work. staging and committing Xcode-managed files is ok. Update CLAUDE.md, rules, docs about this."*. The boundary + carve-out remain canonical verbatim — exact same user-direct phrasing as 2026-06-25. Confirmed no drift in `CLAUDE.md` § Xcode Agent Safety or `.claude/rules/xcode-agent-safety.md` § "Staging + committing Xcode-managed files IS allowed" after a clean read this round. Round PRs in `Docs/IMPLEMENTATION_HANDOFF.md` seventh-mid-session entry (Track A docs reaffirm / Track B ForgeKit pin bump handoff / Track C Localizable.xcstrings handoff / Track D VoicePatternHistoryService / Track Z round-close + session handoff). The auto-cycle runs 5 times cleanly (branch → commit → push → PR → merge → verify). Two NEW user-GUI handoffs filed this round (`HANDOFF_TO_USER_FORGEKIT_PIN_BUMP.md` + `HANDOFF_TO_USER_LOCALIZABLE_XCSTRINGS.md`); neither involves agent authoring of an Xcode-managed file. No new privileged surfaces beyond the existing handoffs.
>
> **Reaffirmed 2026-06-25 (PR 1 of a 4-PR session-handoff Priority-pickup round)** by user-direct: *"critical: do not author/edit xcode-managed files including Xcode workspace file and Xcode scheme/test plan file. Instead, file a handoff doc with the user to do Xcode UI work. staging and committing Xcode-managed files is ok. Update CLAUDE.md, rules, docs about this."*. The boundary + carve-out remain canonical verbatim. No drift in `CLAUDE.md` § Xcode Agent Safety or `.claude/rules/xcode-agent-safety.md` after a clean review this round. Round PRs in `Docs/IMPLEMENTATION_HANDOFF.md` sixth-mid-session entry (Track A docs reaffirm / Track B multi-listener subtext picker / Track C dark + high-contrast palette variants / Track Z round-close). The auto-cycle ran 4 times cleanly (branch → commit → push → PR → merge → verify). No Xcode-managed-file authoring; no new privileged surfaces beyond the existing handoffs.
>
> **Reaffirmed 2026-06-24 (fifth mid-session, PR 4)** by another user-direct multi-PR session-handoff-Priority-pickup round (PRs #115 → #118 in `Docs/IMPLEMENTATION_HANDOFF.md` fifth-mid-session entry). Same canonical boundary + carve-out. The rule survives this round verbatim. Round PR 1 (#115) shipped a new XCUITest file via MCP `XcodeWrite` (synchronized-folder target — zero pbxproj diff) per the standing convention. PR 2 (#116) shipped a pure-docs TestFlight Beta checklist. PR 3 (#117) added `ForgeContent` as a Services dep + wrapped `ForgeContentLoader` for a portfolio-canonical two-tier kit lookup seam; no Xcode-managed-file authoring. PR 4 (this PR) is round-close docs. The auto-cycle ran 4 times cleanly (branch → commit → push → PR → merge → verify). No new privileged surfaces beyond the existing handoffs.
>
> **Reaffirmed 2026-06-23 (fourth mid-session, PR 8)** by yet another user-direct multi-PR App-Store-prep + ForgeKit-active-path + UI-test-smoke round (PRs α.1–δ.1 + final round-close in `Docs/IMPLEMENTATION_HANDOFF.md` fourth-mid-session entry). Same canonical boundary + carve-out. Round PRs α.1–α.3 shipped 3 App-Store-prep docs closing Phase 4 row 163 (privacy nutrition label + KIDSAFE plan + parental gates audit). PR β.1 shipped the voice-acting coach active path as pure SPM source — `VoiceActingCoachActiveSession` (TWO-PART tap rule per concurrency.md) + new `VoiceCoachingSheet` SwiftUI surface + `PerformanceBoothView` integration; safe no-op until the existing voice-coach Info.plist handoff lands. PR β.2 wired Live Activity start/update/end into WriteTabView's lifecycle hooks (`.task(id: nodes.count)` + `.onChange(stage)` + scenePhase background); safe no-op until the existing Widget Extension handoff lands. PR γ.1 filed a fresh hub handoff for the Curating Together Companion Pack PDF. PR δ.1 shipped 5 XCUITest cases for the Performance Booth + Anthology Curation surfaces via MCP `XcodeWrite` (synchronized-folder target convention — no pbxproj diff) + a new `-uiTestUnlockAdventure` launch arg in AdventureTabView. The auto-cycle ran 7 times cleanly. No Xcode-managed-file authoring; no new privileged surfaces beyond the existing handoffs. The rule survives this round verbatim.
>
> **Reaffirmed 2026-06-23 (third mid-session, PR 7)** by another user-direct multi-PR ForgeKit-integration + feature-plan round (PRs 1–7 in `Docs/IMPLEMENTATION_HANDOFF.md` third-mid-session entry). Same canonical boundary + carve-out. The rule is restated verbatim on every reaffirmation. Round PRs 1–6 shipped pure SPM source + Docs + tests + 2 new user-GUI handoff docs (Voice-Acting Coach Info.plist + Widget Extension target); none required Xcode-managed-file authoring. PR 4's SwiftData model addition went through MCP `XcodeUpdate` for the app-shell `DialogueQuestApp.swift` (synchronized-folder target convention). The auto-cycle ran 6 times cleanly (branch → commit → push → PR → merge → verify) per the standing carve-out for non-Xcode-managed work.
>
> **Reaffirmed 2026-06-23 (second mid-session, PR E)** by another user-direct multi-PR ForgeKit-integration + feature-plan round (PRs A–E in `Docs/IMPLEMENTATION_HANDOFF.md`). Same canonical boundary + carve-out. The rule is restated verbatim on every reaffirmation so a future session opening this doc cold gets the load-bearing version regardless of which round was last. Round PRs A–D shipped pure SPM source + Docs + tests; none required Xcode-managed-file authoring. The auto-cycle ran 4 times cleanly (branch → commit → push → PR → merge → verify) per the standing carve-out for non-Xcode-managed work.
>
> **Reaffirmed 2026-06-23 (mid-session)** by the user-direct multi-PR ForgeKit-integration + feature-plan round. Both the boundary AND the staging-carve-out remain canonical. The rule survives every round — author no privileged file from disk; file `HANDOFF_TO_USER_<TOPIC>.md` instead. Staging + committing the resulting GUI diff IS the agent's job. If a future session adds a new privileged surface (a new entitlement, a new Info.plist key, an asset catalog change), file the fresh handoff — DO NOT author the privileged file directly. The full glob table lives in `@CLAUDE.md` § Xcode Agent Safety + `@.claude/rules/xcode-agent-safety.md`; this doc is the single-page reaffirmation.
>
> **2026-06-23 auto-cycle note** (per saved feedback `feedback_auto_cycle_branch_pr_merge.md`): for multi-commit work the default loop is `branch → commit → push → gh pr create → gh pr merge --merge --delete-branch → verify` with no per-step confirmation prompts. The auto-cycle DOES NOT apply to Xcode-managed-file authoring — those still require a `HANDOFF_TO_USER_*.md` and user-side GUI work. Staging + committing the GUI-authored diff afterward IS in-scope for the auto-cycle.

## What the agent will NEVER do (authoring)

Per `CLAUDE.md` § Xcode Agent Safety + `.claude/rules/xcode-agent-safety.md`:

| Glob | Owned by |
|---|---|
| `*.xcodeproj/project.pbxproj` + `*.xcodeproj/project.xcworkspace/**` | Xcode project editor |
| `DialogueQuest.xcworkspace/contents.xcworkspacedata` + `DialogueQuest.xcworkspace/xcshareddata/**` + `DialogueQuest.xcworkspace/xcuserdata/**` | Xcode workspace editor |
| `*.xcscheme` (anywhere) | Xcode scheme editor |
| `DialogueQuest.xctestplan` at repo root | Xcode test plan editor |
| `*.xcassets/Contents.json` + `*.imageset/Contents.json` | Xcode asset catalog editor |
| `DialogueQuest/DialogueQuest/Info.plist` | Xcode target editor |
| `*.entitlements` | Xcode capabilities editor |
| `*.xcdatamodeld/**` | Xcode data model editor |
| `xcuserdata/**` (anywhere) | Xcode local per-user |
| `**/.swiftpm/**` | Xcode SPM cache |
| `Package.resolved` | SPM resolution |

When the work would require touching any of these, the agent files a `Docs/HANDOFF_TO_USER_<TOPIC>.md` with explicit Xcode GUI steps. The user does the GUI work; the agent commits the resulting diff.

## What the agent WILL do (load-bearing carve-outs)

1. **Author + edit SPM source** under `Libraries/Sources/<Target>/**/*.swift` and `Libraries/Tests/<Target>Tests/**/*.swift` via `Write` / `Edit`.
2. **Edit `Libraries/Package.swift`** to add SPM targets, products, dependencies, and resource directories. Watch out — Xcode triggers package re-resolution on save; land Package.swift edits in isolated commits.
3. **Edit `Apps/DialogueQuest/DialogueQuest/**/*.swift`** (app-shell sources) via MCP `XcodeUpdate` — NOT filesystem `Write` / `Edit`. Synchronized-folder targets tolerate filesystem writes but routing through MCP is canonical-safe.
4. **Edit Docs / scripts / config** — `Docs/*.md`, `.gitignore`, `Resources/**/*.{json,yaml,png,webp,caf,lottie}`, `Scripts/*.{py,sh}` — always safe.
5. **Stage + commit Xcode-managed file diffs** that the USER authored via the GUI. The "do not write" rule applies to AUTHORING content, NOT to `git add` / `git commit` of GUI-authored diffs. When a user completes Steps 1-4 of an Xcode-wiring handoff, the agent's job is to commit the resulting `project.pbxproj` / `xctestplan` / `xcscheme` diff so the handoff loop closes.

## SPM folder taxonomy (load-bearing)

Per `CLAUDE.md` § SPM File Layout Convention. Subfolders auto-discover under SPM — no `Package.swift` edit needed when adding files inside an existing target subfolder.

```
Libraries/Sources/Models/
├── Schema/                               # @Model classes + VersionedSchema + MigrationPlan
├── ValueTypes/                           # nonisolated Sendable structs/enums
└── Models.swift                          # target marker

Libraries/Sources/Services/
├── Analytics/                            # RetentionMetricsService + DialogueQuestAnalytics
├── Analyzers/                            # BranchMeaningfulnessScorer + VoiceConsistencyAnalyzer + TagBalancer + TriangleDynamics
├── Gamification/                         # XPEngine + StreakService + AchievementService + DialogueQuestGamification
├── Pedagogy/                             # DDAEngine + DialogueScaffoldingService + ReturnLoopService + SessionTimerService + VariableReward
├── Persistence/                          # DialoguePersistenceService
├── Privacy/                              # ParentalConsentService + ParentalGateChallenge + DeclaredAgeRangeGate + CrisisResourcesProvider
└── Sensory/                              # DialogueSensoryService (ForgeSensory wrapper; juice-layer first cut)

Libraries/Sources/AIMentor/
├── CastVoicing/                          # CastVoiceRegistry + CastVoiceLayer
├── Fallbacks/                            # PatterFallbacks
├── Generables/                           # @Generable types — one per file
└── PatterMentor.swift                    # session host

Libraries/Sources/AppFeature/
├── Adventure/                            # DialogueQuestHubContribution
├── Anthology/                            # AnthologyGalleryView
├── Builder/                              # DialogueTreeBuilderView + DialogueTreeMachine + WriteTabUITestSeed
├── Dashboard/                            # TagBalanceDashboardView
├── Inspector/                            # NodeInspectorView + CharacterAuthoringView
├── Mentor/                               # CastVoicingChip + CastVoicingService + PatterReactionService
├── Onboarding/                           # OnboardingFlow
├── Panels/                               # SubtextPanelView + BranchMeaningfulnessCheckView
├── Parents/                              # CompanionPackView ← NEW 2026-06-22 PR #61
├── Profile/                              # ProfileDashboardView + ParentProgressDashboardView + SessionCloserSheet
├── Progress/                             # ProgressDashboardView
├── Quiz/                                 # QuizMachine + QuizView + QuestionKit
├── Resources/                            # Questions/*.json + CompanionPack/*.pdf
├── Settings/                             # SettingsView + CrisisResourcesView
├── Tabs/                                 # WriteTabView + AdventureTabView + ProgressTabView + ProfileTabView
├── Together/                             # CollaborativeDialogueSession + CollaborativeDialogueView
├── Welcome/                              # WelcomeBackBannerView
├── RootView.swift
└── AppNavigationMachine.swift
```

When adding a new file, drop it into the correct subfolder by responsibility — not by Swift kind (don't make a `Views/` / `Services/` folder inside an existing target; the subfolders ARE the responsibility partition).

## Currently-open user handoffs (still pending)

These are the GUI items the user (not the agent) needs to act on:

| Doc | What |
|---|---|
| `Docs/HANDOFF_TO_USER_APP_ICON.md` | Generate Patter source PNG → Icon Composer → 6-variant `.icon` bundle in asset catalog. Not blocking TestFlight; required before App Store. |
| `Docs/HANDOFF_TO_USER_DECLARED_AGE_RANGE_API.md` | Add Family Controls entitlement + `NSChildUseDescription` Info.plist key via Xcode GUI. Unblocks parental-consent Phase 2. (Once GUI work lands, the agent scaffolds the `DeclaredAgeRangeGate` Swift surface so the API call is entitlement-gated + safe by default.) |
| `Docs/HANDOFF_TO_USER_VOICE_ACTING_COACH_INFOPLIST.md` | Add `NSMicrophoneUsageDescription` + `NSSpeechRecognitionUsageDescription` Info.plist keys. Unblocks `VoiceActingCoachService` + Performance Booth's "Coach my voice" affordance. |
| `Docs/HANDOFF_TO_USER_WIDGET_EXTENSION.md` | Create Widget Extension target + `NSSupportsLiveActivities` Info.plist key. Unblocks `DialogueWritingSessionActivity`. |
| `Docs/HANDOFF_TO_USER_FORGEKIT_PIN_BUMP.md` | Filed 2026-06-26 — bump ForgeKit pin in `Libraries/Package.swift` from `from: "0.99.0"` to a 1.0.0-rc.x-aware constraint (e.g. `.exact("1.0.0-rc.3")` or `.upToNextMajor(from: "1.0.0-rc.3")` if you want patch-level updates). Unblocks `ForgeMasteryEngine` + `PolyaScaffold` integration. **Verified still pending 2026-07-06** — `Libraries/Package.swift` line 26 still reads `from: "0.99.0"`. |
| `Docs/HANDOFF_TO_USER_LOCALIZABLE_XCSTRINGS.md` | Filed 2026-06-26 — File → New File → String Catalog → save as `Libraries/Sources/AppFeature/Resources/Localizable.xcstrings`. Pre-App-Store localization seam; unblocks `ForgeLocalization` adoption. **Verified still pending 2026-07-06** — `Libraries/Sources/AppFeature/Resources/Localizable.xcstrings` still absent. |

Two prior items closed earlier in 2026-06-21: `HANDOFF_TO_USER_CLEAR_STALE_WORKSPACE_GROUP.md` (stale group removed) + `HANDOFF_TO_USER_ADD_AIMENTORTESTS_TO_TESTPLAN.md` (test plan now includes `AIMentorTests`). Verify-before-action: if the user reports either issue resurfacing, re-pull the test plan + workspace data via `git pull` before re-filing.

A third user handoff lands in the 2026-06-23 round: `HANDOFF_TO_HUB_PATTER_APP_ICON_PNG.md` — request to hub for the 1024×1024 Patter source PNG that the App Icon handoff depends on (asset-generation ownership lives with hub per `@.claude/rules/portfolio.md` § "Asset generation ownership"). Once the PNG lands in `Resources/Art/`, the App Icon handoff's blocker is cleared and Icon Composer GUI work can run.

When the user finishes any of these, the agent's role is to stage + commit the resulting diff. Confirm the test plan + scheme are loading correctly via `BuildProject` afterwards.

## Cross-references

- `CLAUDE.md` § Xcode Agent Safety (load-bearing)
- `.claude/rules/xcode-agent-safety.md` — full rule
- `Docs/HANDOFF_TO_USER_XCODE_WIRING.md` — canonical 5-step wiring sequence (closed)
- `Docs/FEATURE_PLAN.md` — phased roadmap; each phase's exit criteria
