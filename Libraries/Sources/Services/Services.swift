import Foundation
import Models

/// Stub target marker for `Services`. Real services ship in subsequent files
/// (DialoguePersistenceService, AnalyticsService, etc.) per Phase 1.
public enum ServicesTarget {
    public static let bundleIdentifier: String = "com.sparkanvil.dialoguequest.services"
}
