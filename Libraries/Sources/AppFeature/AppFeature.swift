import Foundation
import SwiftUI

/// Stub target marker for `AppFeature`. Real root view + navigation ship in
/// subsequent files (RootView, DialogueQuestTab, etc.) per Phase 1.
public enum AppFeatureTarget {
    public static let bundleIdentifier: String = "com.sparkanvil.dialoguequest.appfeature"
}
