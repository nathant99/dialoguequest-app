import Foundation
import Models

/// Stub target marker for `AIMentor`. Real mentor + @Generable types ship in
/// subsequent files (PatterMentor, DialogueLineAnalysis, etc.) per Phase 1.
public enum AIMentorTarget {
    public static let bundleIdentifier: String = "com.sparkanvil.dialoguequest.aimentor"
}
