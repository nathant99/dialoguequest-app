import Foundation
import SwiftUI
import ForgeUI

/// Stub target marker for `SharedUI`. Real components ship in subsequent files
/// (DialogueQuestTheme, DialogueTreeBuilderView, etc.) per Phase 1.
public enum SharedUITarget {
    public static let bundleIdentifier: String = "com.sparkanvil.dialoguequest.sharedui"
}
